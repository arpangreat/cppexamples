cmake_minimum_required(VERSION 2.8.9)
project(newCpp)
include_directories(include)
file(GLOB_RECURSE SOURCES "src/*".cc)
add_executable(newCpp ${SOURCES})
install(TARGETS newCpp DESTINATION /usr/bin)
